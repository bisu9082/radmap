# Versions verified to run the provided scripts (this environment).
# Regenerate on your machine with: pip freeze | grep -Ei 'numpy|scipy|pandas|pykrige'
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
pykrige==1.7.3
matplotlib   # for figures (version as installed)
